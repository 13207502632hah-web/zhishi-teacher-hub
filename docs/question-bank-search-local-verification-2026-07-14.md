# 政治题库检索优先：本地候选版验证报告

日期：2026-07-14
范围：仅本地开发与验证；未提交、未推送、未部署、未发布。

## 一、已完成范围

- 将“题库检索”和“组卷草稿”提升为主导航，首页提供“导入 Word、继续校对、搜索题目、开始组卷”四个入口；小程序及低频工具移入“更多工具”，原功能未删除。
- 题库页支持多 DOCX 顺序导入、逐文件状态、单文件失败后继续、失败重试、刷新后恢复队列和已完成任务链接。
- Word 解析补充材料、题干、选项、答案、解析、采分点、规范表述、大题标题、图片和表格的题号归位；无法确认的位置使用“【存疑】”。
- 完全重复题自动跳过；相似题只提示并提供对照，不自动删除。
- 建立“学段→年级→教材版本→册别/模块→单元→课框→知识点”级联筛选，并支持关键词高亮、快捷标签、清空、最近搜索、保存筛选和多种排序。
- 题目卡片支持答案解析展开、收藏、批量标签、加入当前试卷草稿；试卷草稿可跨刷新恢复。
- 新增本地迁移 `0021_question_bank_search.sql`、常用搜索索引、个人保存筛选表及读取、保存、删除接口。

## 二、数据安全检查

- 执行迁移前已备份本地 D1：`.wrangler/backups/before-0021-question-bank.sqlite`。
- 迁移前后本地正式题目均为 13 道，没有改写或删除已有题目。
- SQLite 完整性检查：`ok`。
- 已确认 4 个新增题目搜索索引和保存筛选表存在。
- 保存筛选的新增、更新、删除仅在 `/tmp` 数据库副本中验证，未向本地正式数据写入测试筛选。
- 1000 道合成题性能测试仅在 `/tmp` 数据库副本中执行；组合筛选并按使用次数排序的本机耗时为 `0.00s`，满足 1 秒目标。

## 三、自动化验证结果

| 检查项 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过 |
| `pnpm lint` | 通过，0 错误；保留小程序脚本原有 1 条未使用变量警告 |
| `pnpm build` | 通过；新增筛选方案、题库检索、级联选项接口均进入构建路由 |
| `pnpm test` | 通过，14/14 |
| 核心逻辑测试 | 通过，25/25 |
| `git diff --check` | 通过 |
| 3 份现有 DOCX 解析 | 通过，分别识别 3、6、3 道题 |
| 本地首页 | `http://localhost:3000` 返回 200 |
| 未登录接口边界 | 保存筛选和级联选项接口返回 401，符合权限要求 |

## 四、WPS 人工回归

已生成并用本机 WPS 打开：

- `.artifacts/question-bank/question-bank-student.docx`
- `.artifacts/question-bank/question-bank-analysis.docx`

人工检查结果：

- 中文字体在 WPS 中正常显示。
- 学生版包含题号、选项、答题线、图片和表格。
- 解析版能显示答案、解析和知识点。
- 强制分页生效，第三部分从新页开始。
- 两个 DOCX 均通过压缩包结构完整性检查。

说明：LibreOffice 自动渲染环境未找到文档指定的华文字体，自动 PDF 预览出现字体替代；WPS 实际打开显示正常，因此以 WPS 人工结果为验收依据。

## 五、尚未执行与上线闸门

- 未使用真实教师密码进行登录，也未读取任何生产密码或密钥；因此本轮没有用真实账号写入保存筛选或正式试卷数据。
- 未批量导入莫老师的真实题库。应先抽取 3—5 份代表文件验收，再决定是否导入全部资料。
- 未提交 Git、未推送、未部署、未发布网站；当前仅为本地候选改动。
- 小程序候选代码保持原样，本轮未继续开发。
