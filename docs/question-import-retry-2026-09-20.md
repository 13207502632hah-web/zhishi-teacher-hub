# 试卷导入修复与实测记录（2026-09-20）

## 实测证据

- 定心卷题卷 8 页、答案卷 4 页，原任务停在 3/12 页；任务尚未入库。
- 原模型首页仅识别 1、2 题，原图还包含第 3、4 题。
- MiMo 文本连通性探测约 39 秒返回 HTTP 500；同账号 Kimi 文本探测约 2.2 秒成功。
- Kimi 首页原文提取约 16 秒返回 1–4 题；第 4 页约 31.9 秒返回 14–19 题。原 25 秒限时会中止正常响应。

## 修改文件与用途

| 文件 | 修改 |
| --- | --- |
| `app/lib/v2/job-service.ts` | 显式重试重置耗尽次数、取消标记、等待时间和租约，保留断点；同一操作编号不重复重置 |
| `app/api/v2/jobs/[id]/route.ts` | 网站重试统一调用恢复逻辑，并校验操作编号 |
| `app/api/v2/questions/imports/automation/route.ts` | 现有题库专用自动化入口支持原任务重试，限制题库任务并记录审计 |
| `app/v2/approvals/ApprovalCenter.tsx` | 重试请求携带操作编号，未改布局 |
| `app/v2/schedule-imports/ScheduleWorkspace.tsx` | 共用重试调用补充操作编号，未改课表业务或布局 |
| `app/lib/v2/ai-router.ts` | 支持提取任务显式关闭推理；即使 JSON 可解析也拒绝截断输出 |
| `app/lib/v2/background-dispatch.ts` | 租约延长至 120 秒，覆盖完整单页请求 |
| `app/lib/v2/question-import-service.ts` | 逐页原文提取，不推测教材标签；保留页末未完题；单页限时 60 秒；合并后核对答案题号，拦截漏题 |
| `tests/v2-background-jobs.test.mjs` | 验证失败/取消恢复、断点保留、重复操作与跨账号边界 |
| `tests/question-import-pages.test.mjs` | 验证缺题对账、截断拒绝、提取请求与跨页续接 |
| `tests/question-import-automation.test.mjs` | 更新逐页协议和执行时间契约 |

线上识别配置拟切换为已实测可用的 `kimi-k2.6`。未增加依赖、数据库迁移或人工复核步骤。

## 提交前检查

- `npm run lint`：通过。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `node --test tests/*.test.mjs`：440/440 通过（使用本轮生产构建）。
- `git diff --check`：通过。
- 相关页面手动测试、浏览器控制台、移动布局：未完成，浏览器连接服务返回 `nodeRepl.fetch request failed`，未绕过或伪造验收结果。

## 尚待线上核对与风险

- 修复发布后仍需重试原任务，并核对 32 题、32 份答案及跨页选项。
- 模型输出具有不确定性；题号对账能发现相对答案卷的漏题，不能证明所有图文和答案语义均正确。
- 本轮不宣称关闭页面后的后台调度已经验证；需另行检查托管环境定时任务交付。
- 技能托管目录在本轮中途不可读；v101 使用已核对的 `dist` 构建输出打包，包含运行入口、托管配置和迁移文件，不含环境变量或密钥。

## v101 上线后的原卷核对

- v101 已上线；原失败任务在原任务编号下恢复，完成 12 页识别和 32 道新题入库，无重复入库。
- 仅有非空答案并不代表答案完整：第 25–29 题的答案被抄成了“示例”。
- 第 4、30 题跨页重号片段被错误择一，导致题干或材料丢失；第 8 题缺少后两项；第 5 题缺组合陈述；第 3 题缺图表文字。
- 学段元数据在逐页检查点中丢失，触发旧的高中默认值。原卷为 2026 年初中中考道德与法治卷，应按初中九年级处理。
- 已使用 PDF 技能对照全部 8 页题卷和 4 页答案卷，准备基于原文修正，不生成替代答案。

## v102 提交范围：导入完整性及原卷修正

| 文件 | 修改 |
| --- | --- |
| `app/lib/v2/question-import-service.ts` | 保留逐页元数据；合并同题跨页片段；校验题干、四选项和组合陈述；示例标签不得算答案；保留识别检查点；扩大任务结果字段便于对账 |
| `app/lib/v2/question-import-correction.ts` | 新增窄范围原卷修正，限定本次自动导入且未被编辑或人工确认的题目；版本冲突保护、原题号保护、幂等、前后值审计及向量刷新 |
| `app/api/v2/questions/imports/automation/route.ts` | 在既有专用授权入口增加完成任务的原卷修正操作 |
| `tests/question-import-pages.test.mjs` | 跨页合并、学段传递、缺项拦截、答案标签与原始续文协议测试 |
| `tests/question-import-correction.test.mjs` | 真实 SQLite 修正、审计、重复请求、跨试卷及人工编辑保护测试 |
| `tests/question-import-automation.test.mjs` | 更新逐页协议版本契约 |
| 本文档 | 记录实测结果、提交范围和未完成验收 |

不新增依赖或数据库迁移，不修改页面布局。计划对原任务的 32 题做一次原文修正，不重复导入整卷，不覆盖教师已编辑内容。

提交前（含新增组合陈述校验）全套 lint、typecheck、build、445 项测试再次通过。页面手动、控制台和移动端检查仍受浏览器连接不可用影响，不能算通过。尚未验证下一份真实 PDF 在新版本上的全自动结果，也未验证生产定时调度；本轮修正不更新历史导入报告，原始报告保留为当时识别结果，修正另有审计记录。

## v102 实测与 v103 修复

- v102 已上线。任务 `17c22c7f-5418-4b52-a09e-5951ef34c00f` 的 32 道题已按原卷修正；回读核对全部修改字段一致，32 份实际答案、24 道完整四选项题、32 道知识点标记，总分 100 分，全部为初中九年级。
- 题库当前活动试卷集为 72 个，关联题目合计 1762 道；这不是对全部旧题质量的验收结论。
- 修正请求返回误报冲突，但数据已经写入。原因是 D1 修改计数包含全文索引触发器写入，不能按 `changes === 1` 判断单题成功。
- v103 仅修改 `question-import-correction.ts`、对应测试和本文档：以同操作逐题审计回执作为完成依据；先保存内容写入结果，再刷新索引；重复请求可以补完索引，但不会再次覆盖内容。
- 新增真实 SQLite 全文索引触发器及索引中断续跑测试。lint、typecheck、build、446/446 测试通过。页面、控制台和手机布局仍未能手动验证。
- 还发现该次 Windows 自动导入的试卷名称、来源文件名乱码；下一步需要单独核对编码并修正来源显示，不能把它计为已解决。

## v103 线上验证及 v104 标签编码修复

- v103 已发布；使用同一操作编号回放原修正请求，返回 32 道已更新、0 冲突和 `repeated: true`，已补完检索向量刷新，没有再次覆盖题目内容。
- v104 单独处理文件标签编码：`scripts/import-question-docx.mjs` 的 Windows 子进程标准输入改用 ASCII Base64 传送 UTF-8 JSON，HTTP 请求体显式使用 UTF-8 字节及字符集。
- 新增 `app/lib/v2/question-import-labels.ts`：按原文件指纹、原名称和原来源匹配修正试卷标签，关联题目仅替换仍等于原错误标签的来源，保留教师自定义标签和正文；修正有审计、冲突保护、重复请求回执及索引刷新。
- 既有 `automation/route.ts` 增加窄范围标签操作；`tests/question-import-correction.test.mjs` 增加标签修正测试；`tests/question-import-windows-encoding.test.mjs` 使用实际 PowerShell 和本地 HTTP 接收端验证中文名称在 GBK 控制台下仍正确；`tests/question-import-automation.test.mjs` 更新字符集契约。
- lint、typecheck、build、448/448 测试通过；无新增依赖或迁移。手动页面、浏览器控制台、移动布局仍未验证。v104 尚待发布并回读中文标签。
