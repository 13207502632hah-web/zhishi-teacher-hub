# V2 旧模块冻结与验收清单

冻结基线：2026-08-09。代码扫描确认旧站共有 30 个 `page.tsx` 页面、全部站点共有
151 个冻结基线 API 路由；V2 已继续扩展版本化业务接口。完整 API 方法、页面调用和测试引用可通过
`pnpm api:inventory` 确定性生成到 `outputs/api-inventory.json`。

状态说明：`V2 原生`表示已具备新版页面和新版接口；`候选桥接`表示数据与权限可用，
但 V2 页面仍跳转旧实现做对账；`公共保留`表示该入口按产品边界继续存在。

| 领域 | 冻结页面 | 当前状态 | 统一切换验收项 |
| --- | --- | --- | --- |
| 公开与登录 | `/`、`/teacher-login` | 公共保留 | 新视觉、登录态、公开/私人资源隔离 |
| 驾驶舱 | `/workspace` | V2 原生 `/v2` | 真实指标、任务、审批与 AI 建议闭环 |
| 课时与日历 | `/lessons`、`/lessons/:id`、`/calendar` | V2 原生列表/创建/详情/私密订阅 | 调课审批、冲突编辑和移动端回归 |
| 课表导入 | `/schedule-imports` | V2 原生、租约后台消费者 | 10/100/500/2000 行、续跑与撤销已通过；待真实图片/PDF OCR 95% 标注集 |
| 题库 | `/questions` | V2 原生检索、导入、逐题待校对、转正审批与选题篮 | 1k/5k/20k/50k 规模、Recall@20、重复题人工合并 |
| 组卷 | `/papers`、`/papers/:id` | V2 原生列表、选题建卷、详情和鉴权导出 | AI 选题理由、总分与梯度校验、PDF 分页真机回归 |
| 学生与班级 | `/students`、`/students/:id`、`/classes`、`/classes/:id` | V2 原生列表、创建、详情编辑、班级成员、助教独立登录与逐班授权 | 独立测试域逐角色动态越权回归 |
| 作业与反馈 | `/assignments`、`/feedback`、`/feedback-imports` | V2 原生草稿、附件、发布审批、AI 批改草稿、批注/语音批改审批、反馈发送和反向建课审批 | 完整订正、上传中断和语音真机回归 |
| 测评与识别 | `/assessments`、`/assessments/:id`、`/exam-projects`、`/recognition` | V2 原生列表、成绩表、后台视觉识别、逐题校对和正式写入审批 | 真实答题卡、原图裁切和证据链真机回归 |
| 教研与学情 | `/analytics`、`/reflections` | V2 原生指标、反馈与反思 | 真实证据、置信度和规模回归 |
| 学年 | `/academic-years` | V2 原生快照预览、排除项、冲突阻断、统一审批和 24 小时安全撤销 | 独立测试域规模与并发变化回归 |
| 资源 | `/resources`、`/resources/:id` | V2 原生列表/私有创建；公开页保留 | 详情编辑、公开资源新视觉与越权测试 |
| 财务 | `/finance` | V2 原生对账、规则重算与审批后入账 | 实收更新、异常处置和规模回归 |
| 系统与小程序 | `/settings`、`/mini-settings` | V2 原生成员/AI/审计；小程序待外部验收 | 正式 AppID、隐私清单、会话停用与真机 |
| 学习门户 | `/portal` | 小程序 V2 接口已接入；网页保留 | 学生/家长角色隔离、多孩子与弱网恢复 |

## API 验收规则

- 每次冻结或候选发布前运行 `pnpm api:inventory`，不得出现无法解释的新路由或无任何
  页面/测试引用的写接口。
- `/api/v2/*` 必须执行权限、幂等、Schema 校验和审计；私有文件只通过鉴权接口读取。
- 旧 API 在统一切换前保持兼容但不新增功能；新业务规则只进入版本化 V2 服务层。
- 小程序不得复制教师端规则，客户端旧路径只在 `mini-program/utils/api.js` 内集中映射。
