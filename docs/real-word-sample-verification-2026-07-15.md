# 真实政治题库 Word 验收与本地导入报告

日期：2026-07-15
范围：验收两份代表性 DOCX，并通过网站现有接口导入本地 D1/R2 待校对区。未修改原文件，未进入正式题库，未提交、未推送、未发布公网。

## 一、WPS 与页面验收

- 高一文件已在 WPS 中核对首页标题、首题、第二题表格和第 9 题漫画；中文正文、题号、表格与漫画相邻关系正常。
- 八下文件已在 WPS 中核对首页标题、首题和第二题；中文正文与题号显示正常。
- 两份原始 DOCX 另用 LibreOffice 渲染为共 98 页图片：高一 58 页、八下 40 页；全部页面已通过缩略图巡检，没有发现整页丢失。
- LibreOffice 缺少原文使用的部分中文字体，因此中文字形以 WPS 抽查为准，98 页渲染主要用于结构、分页、图片和表格巡检。
- 两份文件首道可识别题号之前各有一个图片对象，WPS 首页未看到明确属于第 1 题的前置图片。系统按安全规则保留“【存疑】”，等待人工最终判断，不擅自删除或归类。

## 二、解析结果

| 文件 | 源文件识别 | 题型 | 答案 | 解析 | 知识点 |
| --- | ---: | --- | ---: | ---: | ---: |
| `高一下期末复习（一） (答案版.docx` | 100 | 单选题 78、材料题 22 | 100/100 | 100/100 | 100/100 |
| `八下选择题专练.docx` | 106 | 单选题 106 | 106/106 | 106/106 | 106/106 |

两份文件均保留原题号。系统没有自动补写答案、解析或知识点。

八下文件采用“前半部分题目、后半部分参考答案与详解”的结构。解析器已经：

- 在“参考答案”标题处切分题目区与答案区；
- 将后置答案、知识点和详解按原题号合并回题目；
- 支持 `27．B  28．C  29．A  30．D` 这类合并答案行；
- 将图片和表格只按题目区的相邻题号归位；
- 保留 `sourceQuestionNumber` 和所有 `importNotes`，写入 D1 时转换为“原题号”和校对备注。

## 三、本地网站导入结果

| 任务 ID | 文件 | 源题数 | 写入待校对 | 完全重复跳过 | 状态 |
| ---: | --- | ---: | ---: | ---: | --- |
| 11 | 高一下期末复习（一） (答案版) | 100 | 100 | 0 | `review` |
| 12 | 八下选择题专练 | 106 | 88 | 18 | `review` |

- 八下跳过的 18 道均为该文件内部的完全重复题，已写入重复报告；不是相似题自动删除。
- 两个任务合计新增 188 道待校对题。本地 D1 现有 201 道题，其中本轮待校对 188 道。
- 188/188 道均保留答案、解析、知识点和原题号。
- 两个任务各有 1 道保留“【存疑】”备注。
- 高一 6 道、八下 6 道含图片；图片已转存本地 R2，D1 只保存鉴权引用，D1 内联图片数为 0。
- 高一 8 道、八下 6 道含表格并保留结构化表格数据。
- 两个原始 DOCX 均已存入本地 R2，源文件接口返回 200，大小分别为 900452 字节和 3207110 字节。
- 本地首页返回 200；两个导入任务详情接口均返回 200；D1 `integrity_check` 为 `ok`。

## 四、导入中发现并修复的问题

八下第 76 题含 4 张图片，旧流程将图片 Base64 原文直接写入 D1，触发 `SQLITE_TOOBIG`。接口按设计回滚了失败题组，没有留下半成品。

本轮修复后：

- 导入接口把 PNG/JPEG 图片转存到本地 R2，D1 只保存 `storageKey`、类型和大小；
- 题组写入失败时同步清理本轮新增的 R2 图片，避免孤儿附件；
- Word 导出同时兼容旧内联图片和新的 R2 图片引用；
- 导入时持久化原题号和所有存疑说明；
- 新增回归约束，防止图片再次直接进入 D1。

## 五、自动检查

- 类型检查：通过。
- Lint：0 错误；小程序暂停代码保留原有 1 条未使用变量警告。
- 核心逻辑测试：28/28 通过。
- 完整构建：通过。
- 页面与接口回归：14/14 通过。
- `git diff --check`：通过。

## 六、结论与下一道闸门

两份真实 Word 已完成 WPS/渲染联合验收，并安全导入本地网站待校对区。它们尚未进入正式题库，也没有发布到公网。

下一步只需在网站待校对页面人工抽查：高一至少 10 道、八下至少 10 道，重点覆盖带图片、带表格、合并答案题以及两道“【存疑】”题。抽查确认后再分批转入正式题库；公网发布仍需莫老师单独确认。
