import { env } from "cloudflare:workers";import { miniDenied,requireMini } from "../../../lib/mini-auth";
export async function GET(request:Request){const access=await requireMini(request);if(miniDenied(access))return access;const p=new URL(request.url).searchParams,requested=Number(p.get("studentId")||0);let studentId=access.studentId;if(access.role==="parent"){const link=await env.DB.prepare("SELECT student_id AS studentId FROM parent_student_links WHERE parent_account_id=? AND student_id=COALESCE(NULLIF(?,0),student_id) AND status='active' LIMIT 1").bind(access.accountId,requested).first<{studentId:number}>();studentId=link?.studentId||null}if(!studentId)return Response.json({error:"尚未绑定学生"},{status:403});const [lessons,feedback,results,finance]=await env.DB.batch([env.DB.prepare("SELECT l.date,l.start_time AS startTime,l.end_time AS endTime,l.location,l.course_name AS courseName,l.topic,l.status FROM lessons l JOIN enrollments e ON e.class_id=l.class_id WHERE e.student_id=? AND e.status='active' AND l.date>=date('now','-30 day') ORDER BY l.date,l.start_time").bind(studentId),env.DB.prepare("SELECT f.lesson_id AS lessonId,f.short_content AS content,f.sent_at AS sentAt,l.date FROM feedback f LEFT JOIN lessons l ON l.id=f.lesson_id WHERE f.student_id=? AND f.status IN ('confirmed','sent') ORDER BY f.created_at DESC LIMIT 20").bind(studentId),env.DB.prepare("SELECT a.title,a.date,a.total_score AS totalScore,ar.score,ar.weak_knowledge AS weakKnowledge FROM assessment_results ar JOIN assessments a ON a.id=ar.assessment_id WHERE ar.student_id=? ORDER BY a.date DESC LIMIT 12").bind(studentId),env.DB.prepare("SELECT l.date,l.course_name AS courseName,lb.billing_factor AS hours,lb.unit_fee AS unitPrice,lb.amount,lf.received_amount AS received,lp.balance_hours AS packageBalance FROM lesson_billing_items lb JOIN lesson_finance lf ON lf.id=lb.lesson_finance_id JOIN lessons l ON l.id=lf.lesson_id LEFT JOIN lesson_packages lp ON lp.student_id=lb.student_id WHERE lb.student_id=? AND lf.payer_type='parent' ORDER BY l.date DESC LIMIT 30").bind(studentId)]);return Response.json({studentId,lessons:lessons.results,feedback:feedback.results,results:results.results,finance:finance.results})}
