"use client";import { useEffect,useState } from "react";import { AppShell } from "../components/AppShell";
export default function MiniSettingsPage(){const[students,setStudents]=useState<any[]>([]),[studentId,setStudentId]=useState(""),[role,setRole]=useState("parent"),[invite,setInvite]=useState<any>(null),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/students").then(r=>r.json()).then(d=>setStudents(d.students||[]))},[]);const create=async()=>{const r=await fetch("/api/mini/invites",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({studentId:Number(studentId),role})}),d=await r.json();if(!r.ok){setMessage(d.error||"生成失败");return}setInvite(d);setMessage("邀请码已生成，由学生或家长在小程序中输入后完成绑定")};return <AppShell title="微信小程序" subtitle="一期作业闭环与学生、家长绑定"><section className="securityBanner"><div><b>当前为开发预览阶段</b><span>尚未配置正式 AppID，不会提交微信审核或对外发布。</span></div><p>正式接入前需完成主体注册、合法域名、AppID、密钥和订阅消息模板配置。</p></section><section className="panel"><div className="panelTitle"><div><p>教师确认绑定</p><h2>生成7天有效邀请码</h2></div></div><div className="formGrid"><label>学生<select value={studentId} onChange={e=>setStudentId(e.target.value)}><option value="">请选择</option>{students.map(s=><option value={s.id} key={s.id}>{s.name} · {s.grade}</option>)}</select></label><label>绑定身份<select value={role} onChange={e=>setRole(e.target.value)}><option value="parent">家长</option><option value="student">学生</option></select></label></div><button className="primaryButton" disabled={!studentId} onClick={create}>生成邀请码</button>{invite&&<div className="metricCard"><span>{role==="parent"?"家长":"学生"}邀请码</span><b>{invite.code}</b><small>有效期至 {String(invite.expiresAt).slice(0,16)}，使用一次后失效</small></div>}{message&&<div className="saveToast">{message}</div>}</section><section className="panel"><h2>一期范围</h2><p>整张试卷布置、多图提交、家长代交、文字/语音/圈画批改、订正版本和隐私遮罩后的优秀作业。无聊天、点赞、评论或排名。</p></section></AppShell>}
