"use client";import {useParams} from "next/navigation";import {useEffect,useState} from "react";import {AppShell,EmptyState} from "../../components/AppShell";
export default function PaperDetail(){const {id}=useParams<{id:string}>(),[d,setD]=useState<any>(null);useEffect(()=>{fetch(`/api/papers/${id}`).then(r=>r.json()).then(setD)},[id]);const print=async()=>{if(!confirm("确认打印或导出这份试卷草稿？"))return;await fetch("/api/audit",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"print",entityType:"paper",entityId:id})});window.print()};if(!d?.paper)return <AppShell title="试卷详情"><EmptyState title="正在读取试卷" description="请稍候…"/></AppShell>;return <AppShell title={d.paper.title} subtitle={`${d.paper.type} · ${d.questions.length}题 · ${d.paper.total_score||0}分`} actions={<button className="primaryButton" onClick={print}>打印 / 导出</button>}><section className="paperDocument"><header><h1>{d.paper.title}</h1><p>姓名：________　班级：________　日期：________</p></header>{d.questions.map((q:any,i:number)=><article key={q.id}><h3>{i+1}．{q.stem}<span>（{q.paperScore||q.score||0}分）</span></h3>{q.material&&<blockquote>{q.material}</blockquote>}{q.options&&<pre>{q.options}</pre>}<div className="answerBlock"><b>答案：</b>{q.answer||"待补充"}<br/><b>解析：</b>{q.analysis||"待补充"}</div></article>)}</section></AppShell>}
